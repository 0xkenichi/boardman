{
  "name": "@afm/engine-adapter",
  "version": "0.1.0",
  "private": true,
  "description": "Adapter: converts the Python AFM engine's MatchResult (feed + phases) into schema-valid @afm/match-contracts MatchOutput. Validation at the boundary; nothing here simulates.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@afm/match-contracts": "file:../match-contracts"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
