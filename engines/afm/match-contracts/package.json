{
  "name": "@afm/match-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Phase 0 contracts for the AFM match engine: types + runtime validation for engine input/output. No simulation logic, no rendering, no networking.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
